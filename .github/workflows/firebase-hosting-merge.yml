# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Optional: If you're using a build system like React/Vite/Next.js, you can put build commands here
      # For now, we'll just skip the build step
      - name: Skip build step
        run: echo "No build step required"

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BEU_B_TECH_PYQ }}
          channelId: live
          projectId: beu-b-tech-pyq
